.modal{style: "display: block"}
	.modal-dialog
		.modal-content
			.modal-header
				%button.close.close-modal X
				%h3.green-text.modal-title Paga con tu tarjeta
			=form_tag "/payments/cards", method: :post, class: "top-space" do
				.modal-body
					.form-group
						=email_field_tag :email,nil, class: "form-control floating-label", placeholder: "Correo electronico", required: "required"
					.form-group
						=text_field_tag :number,nil, class: "form-control floating-label", placeholder: "Numero de tarjeta", required: "required"
					.form-group.top-space
						%p.be-small Fecha de expiracion
						.col-xs-6.col-sm-4.col-md-3.no-padding.inline-block
							=select_tag :expire_month, options_for_select((1..12).to_a), class: "form-control"
						.col-xs-6.col-sm-4.col-md-3.no-padding.inline-block
							-year = DateTime.now.year
							=select_tag :expire_year, options_for_select((year..year+8).to_a), class: "form-control"			
					.form-group.top-space
						=number_field_tag :cvv2,nil, class: "form-control floating-label", placeholder: "CVV", required: "required"
					%p.top-space.green-text * Los datos de tu tarjeta no son almacenado
				.modal-footer
					=content_tag :button, type: :submit, class: "btn btn-primary" do
						Pagar	
				
